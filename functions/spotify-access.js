exports.handler = (event, _context, callback) => {
    // console.log({ event });

    // callback(null, {
    //     statusCode: 200,
    //     body: JSON.stringify({ boop: true })
    // });

    // TODO: 

    // 1. authorize and receive access token

    // 2. use token to receive data

    // 3. output data on this page so it can be accessed
};